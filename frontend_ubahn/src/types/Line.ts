export interface Line {
  name: string;
  color: string;
}

export interface FullLine {
  name: string;
  color: string;
  type: string;
  stations: string[];
}

export interface AccessibleLine {
  name: string;
  color: string;
}
